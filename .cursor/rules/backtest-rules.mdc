---
description: 回测引擎可信性硬约束 — 所有回测代码必须遵守
globs:
  - backend/app/research/backtest/**
  - backend/app/research/strategies/**
alwaysApply: false
---

# 回测可信性规则

## 信号执行时机 (防偷看未来)

- bar 内信号必须在**下一个 bar 的开盘价**执行，禁止在当前 bar 内成交
- 日线策略: T 日收盘信号 → T+1 日开盘执行
- 分钟策略: 当前 bar 结束信号 → 下一 bar 开盘执行
- 回测引擎严禁提供当前 bar 的 close/high/low 给策略后又在同一 bar 内撮合

## 不可交易情况过滤

- **涨停板买入**: 目标价 >= 涨停价 → 订单取消
- **跌停板卖出**: 目标价 <= 跌停价 → 订单取消
- **一字板**: 涨停/跌停且 open == close → 完全不可交易，双向拦截
- **停牌**: 通过 `suspend_d` 表校验，自动跳过
- **ST 股**: 涨跌停限制改为 ±5% (非 ±10%)
- **新股上市首日**: 不参与交易
- 所有被过滤信号必须记录到审计日志 (FilterReason + 原始信号)

## 复权口径

- 技术指标计算: **后复权** (hfq)，保证价格连续性
- 手续费/滑点计算: **不复权**真实价格
- 收益计算: **后复权**

## 幸存者偏差

- 股票池使用**时点列表** (point-in-time)，禁止用当前上市股票回测历史
- 已退市股票必须包含 (`stock_basic.list_status='D'`)
- 指数成分股使用历史成分 (`index_weight` 按月)

## 未来函数

- 回测引擎每个时间步只能访问 <= 当前时间的数据
- 财务数据以**披露日期**为可用时间点 (非报告期)
- 禁止在策略 on_bar() 中查询未来数据

## 费用与滑点 (A股)

- 佣金: max(成交额 × 万2.5, 5元)，双向
- 印花税: 成交额 × 0.05%，仅卖出
- 过户费: 成交额 × 0.001%，仅沪市 (.SH)，双向
- 滑点: 1 tick (0.01元) 基础 + 成交量冲击 (订单/bar量 > 1% 时加大)
- 最小交易: 100 股 (买入必须整手，卖出可零股)
- 单笔订单 ≤ bar 成交量的 20%

## 分钟级特殊规则 (Phase 4 MVP 暂不实现，留作扩展)

- 1min bar 开盘价需额外加滑点
- 9:30 首 bar 集合竞价跳空需特殊处理
- 午休后 13:00 首 bar 同理
