---
description: AI Trade 量化交易系统 - 全局项目规则
globs:
alwaysApply: true
---

# AI Trade 项目约束

## 执行纪律

- **先读计划**: 每次新对话开始，先读 `.cursor/plans/ai_trade_量化交易系统_725fc656.plan.md`，确认当前步骤和MVP边界。主计划中若有 `> 见 [xxx.plan.md]` 跳转引用，需跟进读取对应子计划文件
- **先读AGENTS.md**: 确认项目当前进度和已完成任务，避免重复劳动
- **冲突处理**: `project-overview.mdc` > 当前激活的计划文件 > `AGENTS.md`；若 `AGENTS.md` 与计划不一致，先更新状态再继续编码
- **不许扩范围**: 严格遵守当前Phase的MVP边界，计划文件中标注"不许越界"的内容绝对不做
- **先复用现有实现**: 新增前先检查 `scripts/`、`backend/app/`、已有模型和端点，禁止重复造轮子
- **写回计划**: 任何新增功能/架构变更必须先写回计划文件，获得确认后再编码
- **更新状态**: 每完成一个Step，更新子计划文件对应 checkbox，同时更新 `AGENTS.md`。若创建了新目录或改变隔离规则，还需更新本文件
- **不重构无关模块**: 只改当前Step涉及的代码
- **改后必验**: 每次完成一个Step后做最小验证，未验证不得标记完成
- **改后必审**: 每次完成一个Step或重大代码变更后，必须调用 code-reviewer subagent 审查代码质量和计划对齐度。详见 `.cursor/rules/code-review.mdc`
- **端口/进程管理**: 后端 `8000`，前端 `5173`。启动前先检查端口占用，被占则先 kill 再启动

## 技术约束

- **Python**: 3.10+，类型注解用 `X | Y`。Windows用 `python` 或 `py -3`
- **数据库**: PostgreSQL 18+，ORM用SQLAlchemy 2.0 (async)，Alembic管理迁移
- **数据源**: Tushare Pro，token从 `TUSHARE_TOKEN` 环境变量读取
- **Web框架**: FastAPI
- **前端**: React 19 + TypeScript + Vite + Ant Design 5 + TailwindCSS v4

## 架构约束

已实现目录:
- `backend/app/core/` — 配置、数据库、Redis连接
- `backend/app/shared/models/` — SQLAlchemy ORM
- `backend/app/shared/interfaces/` — Pydantic models + enums (research/execution 通信协议)
- `backend/app/shared/data/` — DataLoader 统一数据访问层
- `backend/app/research/data/` — TushareService
- `backend/app/execution/` — TradingEngine, OMS, 风控, 撮合, Feed, 可观测性
- `frontend/` — React 19 交易控制台 + Storybook
- `scripts/` — 数据拉取/同步脚本

隔离规则:
- **execution/ 禁止 import research/** 下任何模块
- **research/ 可以 import execution/oms/, execution/matcher.py, execution/fee.py, execution/slippage.py** (回测复用撮合逻辑)
- **research/ 禁止 import execution/engine.py** (不能使用实时交易单例)
- 两层通过 **shared/interfaces/** 定义契约

## 数据库约束

- 常规表用 **Alembic** 管理迁移
- **分区表用原生SQL**: `stock_min_kline` (按月分区)，脚本在 `scripts/create_min_partitions.py`
- Alembic autogenerate 会误删分区子表，迁移后必须检查并删除相关 `op.drop_table`

## 数据时效性约束

- 数据**不会自动更新**，是离线历史快照
- `AGENTS.md` 记录数据覆盖范围和最后拉取时间，**每次拉取后必须更新**
- AI不得假设数据是"实时"或"最新"的；需要最新数据时，提示用户先执行增量更新

## Tushare调用规范

- 所有调用必须经过 `backend/app/research/data/tushare_service.py`，禁止直接 `pro.query()`
- 必须有频次控制和错误重试
- 参考 `.cursor/skills/skill-tushare-data/` 中的接口文档
